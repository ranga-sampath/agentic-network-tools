Network performance tests between tf-source-vm (10.0.1.4) and tf-dest-vm (10.0.1.5) are completely broken — iperf connections are failing. This happened after a change window on tf-dest-vm last night where the platform team applied both an NSG configuration update and OS-level performance tuning. At least one of those changes is causing problems, but our on-call engineer suspects there may be more than one issue. Resource group: nw-forensics-rg. Please do a thorough investigation — we need the complete picture before the post-incident review, not just the first thing you find.
