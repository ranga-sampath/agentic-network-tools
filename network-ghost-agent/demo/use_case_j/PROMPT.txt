Our iperf-based throughput tests between tf-source-vm (10.0.1.4) and tf-dest-vm (10.0.1.5) are completely failing — every connection attempt is refused or hangs. The Azure NSG shows port 5001 is allowed inbound on tf-dest-vm. Both VMs are running and healthy in the portal. This started after the infrastructure team applied OS hardening scripts to tf-dest-vm last night. Investigate the connectivity failure in resource group nw-forensics-rg and identify what is blocking the traffic.
