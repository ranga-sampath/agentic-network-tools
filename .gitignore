#Social media posts or blog posts
posts/

# Conceptual documents
concepts/

# Secrets
.env
*.pem

# Python / Environment
.venv/
__pycache__/
*.py[cod]
.python-version

# Dependencies
# Keep pyproject.toml and uv.lock, but ignore the legacy requirements.txt if not used
# requirements.txt 

# macOS
.DS_Store

# IDEs / AI Tools
.claude/
.vscode/
.idea/
CLAUDE.md

# Application Output (Generated data)
agentic-pcap-forensic-engine/tests/sample_pcaps/

# Ghost Agent — eval artifacts and infrastructure (local only)
network-ghost-agent/eval/

# Ghost Agent — runtime output and secrets
ghost_session.json
audit/
agentic-safety-shell/audit/
agentic-cloud-orchestrator/audit/
agentic-network-ghost-troubleshooter/audit/

# Demo config (contains real API keys and subscription IDs)
network-ghost-agent/demo/config.env

# Cloud Orchestrator — examples and test run reports
agentic-cloud-orchestrator/examples/
agentic-cloud-orchestrator/tests/test-report.md

# Pipe Meter — runtime session artifacts, shell audit logs, secrets, and test reports
agentic-pipe-meter/audit/
agentic-pipe-meter/config.env
agentic-pipe-meter/tests/test-report.md

# Netfilter Inspector — runtime artifacts, local dev configs, and internal plans
netfilter-inspector/config.env
netfilter-inspector/firewall-inspector/config.env
netfilter-inspector/firewall-inspector/multipass-config.env
netfilter-inspector/firewall-inspector/nftables-config.env
netfilter-inspector/firewall-inspector/plans/
netfilter-inspector/firewall-inspector/audit-nft/
netfilter-inspector/test-audit/
netfilter-inspector/iptables-parser/explain-feature-design.md

# Ghost Agent — additional runtime artifact directories
network-ghost-agent/test-audit/
network-ghost-agent/audit-layer4/

# Security Rule Inspector — runtime artifacts
security-rule-inspector/audit/

# Effective Route Inspector — runtime artifacts (also in module's own .gitignore)
effective-route-inspector/audit/

# pytest cache (global)
.pytest_cache/

# Concept documents (local ideation, not source)
concept.md

# uv scaffold placeholder
main.py
