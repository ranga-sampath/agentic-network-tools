# =============================================================================
# Ghost Agent Demo — Resource Configuration
# Copy this file to config.env and fill in your values before running demos.
#
#   cp demo/sample_config.env demo/config.env
#   vi demo/config.env
# =============================================================================

# Azure subscription and resource group
SUBSCRIPTION_ID="<your-azure-subscription-id>"
RESOURCE_GROUP="<your-resource-group-name>"
LOCATION="<azure-region>"                    # e.g. eastus, westus2, australiaeast

# Virtual network
VNET_NAME="<your-vnet-name>"
SUBNET_NAME="<your-subnet-name>"             # e.g. default

# Virtual machines
# SOURCE_VM: the one with a public IP (you SSH from here)
SOURCE_VM_NAME="<source-vm-name>"
SOURCE_VM_PUBLIC_IP="<source-vm-public-ip>"
SOURCE_VM_PRIVATE_IP="<source-vm-private-ip>"   # e.g. 10.0.1.4

# DEST_VM: no public IP, reachable only within the subnet
DEST_VM_NAME="<dest-vm-name>"
DEST_VM_PRIVATE_IP="<dest-vm-private-ip>"       # e.g. 10.0.1.5

# NSG attached to each VM's NIC
SOURCE_VM_NSG_NAME="<source-vm-nsg-name>"
DEST_VM_NSG_NAME="<dest-vm-nsg-name>"

# Storage account and container for packet captures
STORAGE_ACCOUNT_NAME="<your-storage-account-name>"
STORAGE_CONTAINER_NAME="<your-storage-container-name>"   # e.g. pktcaptures

# SSH key for source VM
SSH_KEY_PATH="${HOME}/<path-to-your-pem-key>"
SSH_USER="azureuser"          # Default Azure Linux VM user

# Gemini API key (also used by ghost_agent.py)
GEMINI_API_KEY="<your-gemini-api-key>"

# Demo port used in Use Case A (the "invisible wall" scenario)
DEMO_BLOCKED_PORT="8080"
